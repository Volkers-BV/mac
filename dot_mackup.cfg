# Mackup configuration
# https://github.com/lra/mackup
#
# Mackup backs up and restores application settings via iCloud (or other storage).
# Only listed applications will be synced. Add or remove apps as needed.

[storage]
engine = icloud

# Only sync the apps listed below (safer than syncing everything)
[applications_to_sync]
zed
